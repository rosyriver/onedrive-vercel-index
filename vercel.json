{
    "github": {
        "silent": true
    },
    "buildCommand": "pnpm build",
    "installCommand": "pnpm install",
    "crons": [
        {
            "path": "/api/refresh-auth",
            "schedule": "0 0 * * *"
        }
    ]
}
